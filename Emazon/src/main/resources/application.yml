server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    web: trace
    org:
      springframework:
        web: trace